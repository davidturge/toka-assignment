// export const groupedTasks = tasks.reduce((acc, task) => {
//   const { projectId } = task;
//
//   // If the projectId key doesn't exist in acc, initialize it as an array
//   if (!acc[projectId]) {
//     acc[projectId] = [];
//   }
//
//   // Push the current task into the appropriate project group
//   acc[projectId].push(task);
//
//   return acc;
// }, {});
